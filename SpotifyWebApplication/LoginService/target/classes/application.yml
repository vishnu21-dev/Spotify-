server:
  port: 8081
spring:
  application:
    name: userLogin
  datasource:
    url: jdbc:mysql://localhost:3306/MySpotifyUserDb?createDatabaseIfNotExist=true
    username: root
    password: Chithra04.@
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
      dialect: org.hibernate.dialect.MYSQL80Dialect

